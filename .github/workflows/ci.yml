# Temporary location for CI workflow. Move to .github/workflows/ci.yml to enable on GitHub.
# Use the provided helper script apply_ci.ps1 to move this into place.

name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:

jobs:
  tests:
    name: Tests on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            python-version: '3.11'
            playwright: true
          - os: windows-latest
            python-version: '3.11'
            playwright: true
          - os: macos-latest
            python-version: '3.11'
            playwright: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt -r requirements-dev.txt

      - name: Install Playwright browsers
        if: ${{ matrix.playwright == true }}
        run: |
          python -m playwright install --with-deps

      - name: Architecture invariants (documented + enforced)
        run: |
          python core/architecture_invariants.py
          python -m pytest tests/test_architecture_invariants.py tests/test_layer_contracts.py -m architecture -q --tb=short
        env:
          CI: true

      - name: Golden tasks and domain benchmarks
        run: |
          python evaluation/golden_runner.py --skip-requires
          python -m pytest tests/test_golden_tasks.py tests/test_domain_benchmarks.py -m "golden or benchmark" -q --tb=short
        env:
          CI: true

      - name: Run tests
        run: |
          python -m pytest -q
        env:
          CI: true
